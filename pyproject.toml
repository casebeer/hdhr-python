[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hdhr"
version = "2026.3.0"
dependencies = []
requires-python = ">=3.8"

[project.scripts]
hdhr = "cli:run"

[tool.setuptools.packages.find]
where = ["hdhr"]
